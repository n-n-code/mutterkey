[Unit]
Description=Mutterkey push-to-talk transcription daemon
After=graphical-session.target
PartOf=graphical-session.target

[Service]
Type=simple
ExecStart=%h/.local/bin/mutterkey daemon --config %h/.config/mutterkey/config.json
Restart=on-failure
RestartSec=2

[Install]
WantedBy=default.target
